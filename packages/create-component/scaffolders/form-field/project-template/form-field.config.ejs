import {
    FORM_FIELD_COMPONENTS,
    PLATFORMS,
} from '<%= CUSTOM_FORM_FIELD_MODEL_PACKAGE_NAME %>'

export default {
        <% Object.keys(formFieldConfig).forEach(platform => { %>
        [PLATFORMS.<%= getFormFieldPlatformKey(platform) %>]: { <% Object.entries(formFieldConfig[platform]).forEach(([componentName, component]) => { %> [FORM_FIELD_COMPONENTS.<%= getFormFieldComponentKey(componentName) %>]: './<%= component.moduleFolderPath %><%= componentName %>.<%= component.fileExtension %>',<% }) %>
        },<% }) %>
}

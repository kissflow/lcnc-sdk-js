import React from 'react'

function <%= componentName %>(props)  {

  const {<%= props %>} = props

  return (
    <div>
      <%= componentName %>
    </div>
  );
};

export default <%= componentName %>;
